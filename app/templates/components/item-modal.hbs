<div class="modal">
  <img src={{item.image}} alt={{item.name}}>
  <div class="caption">
    <h3>ATTEMPT MODAL HERE{{item.name}}</h3>
    <p>{{item.description}}</p>
    <ul>
      <li>{{item.size}}</li>
      <li>{{item.price}}</li>
    </ul>
  </div>
  <button {{action ‘addToCart’ product}}>Add to Cart</button>
</div>
